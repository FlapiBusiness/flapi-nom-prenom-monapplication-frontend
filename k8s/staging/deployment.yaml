apiVersion: apps/v1
kind: Deployment
metadata:
  name: staging-NOM PRENOM-MonApplication-frontend-deployment
  namespace: staging-NOM PRENOM-MonApplication-frontend
  labels:
    app: staging-NOM PRENOM-MonApplication-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: staging-NOM PRENOM-MonApplication-frontend
  template:
    metadata:
      labels:
        app: staging-NOM PRENOM-MonApplication-frontend
    spec:
      containers:
        - name: staging-NOM PRENOM-MonApplication-frontend
          image: corentin35/NOM PRENOM-MonApplication-frontend:staging-${TAG_VERSION}
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: '400m'
              memory: '512Mi'
            limits:
              cpu: '800m'
              memory: '1Gi'
      imagePullSecrets:
        - name: regcred
