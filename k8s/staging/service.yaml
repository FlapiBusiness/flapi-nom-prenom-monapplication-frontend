apiVersion: v1
kind: Service
metadata:
  name: staging-NOM PRENOM-MonApplication-frontend-service
  namespace: staging-NOM PRENOM-MonApplication-frontend
spec:
  ports:
    - port: 3000
      targetPort: 3000
  selector:
    app: staging-NOM PRENOM-MonApplication-frontend
