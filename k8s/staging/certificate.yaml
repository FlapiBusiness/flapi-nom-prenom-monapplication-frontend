apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: staging-NOM PRENOM-MonApplication-frontend-cert
  namespace: staging-NOM PRENOM-MonApplication-frontend
spec:
  secretName: staging-NOM PRENOM-MonApplication-frontend-tls
  dnsNames:
    - staging.monsousdomain.flapi.org
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
