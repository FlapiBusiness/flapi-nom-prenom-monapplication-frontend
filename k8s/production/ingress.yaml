apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prod-NOM PRENOM-MonApplication-frontend-ingress
  namespace: prod-NOM PRENOM-MonApplication-frontend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - monsousdomain.flapi.org
      secretName: prod-NOM PRENOM-MonApplication-frontend-tls
  rules:
    - host: monsousdomain.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: prod-NOM PRENOM-MonApplication-frontend-service
                port:
                  number: 3000
